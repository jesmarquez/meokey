import React from 'react'
import TextField from 'material-ui/TextField'

const SearchBox = () => (
  <div className="container-search">
    <TextField
      hintText="Ingrese el servidor"
    />
  </div>
)

export default SearchBox
